<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wufeng.commonmvc">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.TerminalParamsActivity"></activity>
        <activity android:name=".ui.TradeRecordActivity" />
        <activity android:name=".ui.PaymentActivity" />
        <activity android:name=".ui.AllBindCategoryActivity" />
        <activity android:name=".ui.WholesaleTradeActivity" />
        <activity android:name=".ui.AddCategoryActivity" />
        <activity android:name=".ui.CategoryManagerActivity" />
        <activity android:name=".ui.AddMerchantCardActivity" />
        <activity android:name=".ui.BindCardActivity" />
        <activity android:name=".ui.SetTerminalActivity" />
        <activity android:name=".ui.HomeActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>